# MongoDB Connection String
# Get this from MongoDB Atlas: Connect -> Connect your application -> Driver: Node.js -> Copy connection string
# Replace <password> with your database user password and 'myFirstDatabase' with your database name (e.g., jokesweb)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/jokesweb?retryWrites=true&w=majority

# NextAuth Configuration
# Generate a random string for production: openssl rand -base64 32
NEXTAUTH_SECRET=changeme_to_a_random_secure_string

# NextAuth URL
# In development: http://localhost:3000
# In production (Vercel): https://your-project-name.vercel.app (Vercel sets this automatically usually, but good to have)
NEXTAUTH_URL=http://localhost:3000

# Resend Email
RESEND_API_KEY=
EMAIL_FROM="JokesWeb <onboarding@resend.dev>"
